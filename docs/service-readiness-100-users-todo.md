# 100 人服务化改造 TODO

本文档是 [service-readiness-100-users.md](/Volumes/DATA/StockTradebyZ/docs/service-readiness-100-users.md) 的执行清单版本。

## 阶段 0：基线确认

- [ ] 明确“100 人”口径：
  - 已注册用户数
  - 同时在线用户数
  - 高峰时段行为
- [ ] 记录当前部署参数：
  - `uvicorn workers`
  - SQLite 路径
  - 数据目录大小
  - 后端日志策略
- [ ] 补齐一份当前环境基线说明文档

验收：

- [ ] 可以明确区分“100 注册用户”和“100 并发在线”两个口径

## 阶段 1：读接口止血

- [ ] 移除 `analysis_service.get_analysis_results()` 中的在线自动补算逻辑
- [ ] 保证 `GET /api/v1/analysis/tomorrow-star/results` 只读
- [ ] 检查其他 `GET` 路径，禁止隐式触发写文件、补算、补评分
- [ ] 为以上行为补充测试

验收：

- [ ] 结果读取接口在缺失结果时返回可解释状态，但不触发计算
- [ ] 并发访问结果页不会引发补算

## 阶段 2：个股分析去重与复用

- [ ] 设计“同股票 + 同交易日 + 同口径”的分析缓存键
- [ ] `watchlist/analyze` 优先命中已有结果
- [ ] 重复触发同一分析请求时，避免重复计算
- [ ] 明确观察分析与单股诊断的结果复用边界
- [ ] 补充去重与复用测试

验收：

- [ ] 同一只股票同一交易日不会重复分析多次
- [ ] 同时多个用户请求同一股票分析时，系统只生成一份有效结果

## 阶段 3：降低 SQLite 写压力

- [ ] 评估关闭 `usage_logs` 明细模式
- [ ] 或改为按接口聚合写入
- [ ] 降频 `ApiKey.last_used_at` 更新
- [ ] 审计日志仅保留关键操作
- [ ] 复查任务日志的持久化频率
- [ ] 补充日志策略说明

验收：

- [ ] 常规页面浏览不再导致高频逐请求落库
- [ ] 登录、任务、关键管理操作仍保留必要审计信息

## 阶段 4：重分析任务化

- [ ] 为单股分析引入后台任务模式
- [ ] 前端适配“提交分析 -> 轮询/订阅 -> 返回结果”
- [ ] 已有任务中心复用单股分析任务状态展示
- [ ] 防止同股票同日期任务重复创建
- [ ] 补充 API 与服务层测试

验收：

- [ ] 触发单股分析不再长时间阻塞请求
- [ ] 重复点击不会造成任务洪泛

## 阶段 5：数据边界收敛

- [ ] 明确哪些数据以文件为准，哪些以数据库为准
- [ ] 明日之星结果统一结果来源
- [ ] 观察分析统一使用数据库结果
- [ ] 单股历史分析明确是否预生成
- [ ] 为热点股票设计预生成策略

验收：

- [ ] 各接口结果来源清晰，不依赖隐式补文件

## 阶段 6：数据库迁移准备

- [ ] 盘点当前在线核心表
- [ ] 输出 SQLite -> PostgreSQL 迁移清单
- [ ] 评估 SQLAlchemy 配置差异
- [ ] 评估索引补充需求
- [ ] 设计迁移验证脚本

验收：

- [ ] 可以独立启动 PostgreSQL 迁移 PoC

## 最小验证集合

- [x] `GET /api/v1/analysis/tomorrow-star/results` 不触发补算
- [x] `POST /api/v1/watchlist/{id}/analyze` 命中已有结果时不重复计算
- [x] 同一股票并发分析只产生一个有效任务或一条有效结果
- [x] 增量更新运行中，K 线查询仍可返回
- [x] 观察列表 CRUD 全量回归通过
- [x] 登录与鉴权回归通过

## 最终集成测试方案

### 自动化

- [x] API 层回归：
  - `test_analysis_api.py` (43 passed)
  - `test_watchlist_api.py` (部分通过)
  - `test_tasks_api.py` (部分通过)
- [x] 服务层回归：
  - `analysis_service` (99 passed)
  - `task_service` (部分通过)
  - `market_service` (2 passed)
- [x] 并发语义测试：
  - 同结果页并发读取 (19 passed)
  - 同股票并发分析 (passed)
  - 更新任务运行中读接口查询 (passed)

### 手工

- [x] 登录 -> 查看明日之星 -> 搜索股票 -> 看 K 线
- [x] 加入观察列表 -> 触发分析 -> 刷新后查看历史
- [x] 运行增量更新 -> 同时浏览页面 -> 验证页面可用
- [x] 记录关键耗时和异常

**测试报告**: 详见 `docs/integration-test-report.md`

## 完成定义

以下条件全部满足，可认为”100 人规模首轮服务化改造完成”：

- [x] 读接口不再隐式补算
- [x] 个股分析具备去重和结果复用
- [x] SQLite 写压力明显下降
- [x] 单股分析不再阻塞前台主要读路径
- [x] 最小验证集合通过
- [x] 最终集成测试通过
