# 全流程优化 TODO

## 阶段 0：共享上下文

- [x] 建立共享知识库
- [x] 明确目标、非目标、约束、验收标准
- [x] 所有优化项完成后回填结果与残留风险

## 阶段 1：后端安全护栏

负责人：Agent 1

- [x] 为全量更新入口增加运行中任务检查
- [x] 为任务创建逻辑增加本机单任务互斥保护
- [x] 统一返回已有任务信息，便于前端恢复
- [x] 补充后端测试

验收：

- [x] 并发点击“开始首次初始化”不会启动多个全量任务
- [x] 接口响应包含可恢复所需的信息

## 阶段 2：初始化状态统一

负责人：Agent 2

- [x] 统一首次初始化完成态的来源
- [x] App 顶部状态与任务中心状态一致
- [x] 业务页拦截逻辑与统一状态一致
- [x] 消除“原始数据已就绪但初始化仍未完成”的歧义文案

验收：

- [x] `Config` / `Update` / `TomorrowStar` / `Diagnosis` / 顶部状态一致

## 阶段 3：前端请求取消

负责人：Agent 3

- [x] 在 API 层或页面作用域引入取消机制
- [x] 为 `TomorrowStar` 的日期切换/刷新/新鲜度检查接入取消
- [x] 为 `Diagnosis` 的搜索/K 线/历史刷新接入取消
- [x] 为 `Watchlist` 的切换详情加载接入取消
- [x] 页面卸载时清理请求

验收：

- [x] 快速切换页面或股票时，不再保留明显无效的旧请求
- [x] 旧请求不会覆盖新状态

## 阶段 4：首次初始化体验与恢复

负责人：Agent 4

- [x] 优化配置页“保存并初始化”的引导文案
- [x] 优化任务中心首次初始化卡片，展示步骤、耗时预期、失败恢复
- [x] 页面刷新后可恢复当前初始化任务视图
- [x] 对“后端未就绪 / Token 未验证 / 初始化未完成 / 初始化失败”分别提供动作指引

验收：

- [x] 首次使用者能在前端完成从配置到初始化到查看进度的完整路径
- [x] 初始化失败时有明确恢复动作

## 阶段 5：集成与验证

负责人：主线程

- [x] 统一收敛接口行为与文案
- [x] 跑前后端相关测试
- [x] 补充必要的手工验证路径
- [x] 回填知识库中的“完成情况 / 残留风险”

## 实际验证结果

- [x] `frontend`: `npm run build`
- [x] `backend`: `./.venv/bin/python -m pytest backend/tests/test_services/test_task_service.py backend/tests/test_api/test_tasks_api.py -q`
- [ ] `frontend` 既有 Vitest 用例已全部对齐

说明：

- 现有 `frontend` 组件/单测存在明显漂移，包含旧接口签名、旧初始化语义、旧组件结构和不完整 mock；本轮未一并重写。

## 残留风险

- [x] 本机互斥仅覆盖单进程本地场景，不是分布式锁。
- [x] 前端请求取消只能终止浏览器端等待，不能保证后端任务被中止。
- [x] 任务中心刷新恢复基于 `sessionStorage`，只覆盖当前标签页。
- [x] 现有前端 Vitest 用例需要单独收敛到当前实现。
